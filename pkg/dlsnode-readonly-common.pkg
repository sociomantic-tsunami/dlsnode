
from defaults import OPTS, get_desc

# we don't want a suffix for this pkg
if VAR.suffix:
    VAR.name = VAR.name[:-len(VAR.suffix)]

OPTS.update(
    name = VAR.name,
    description = get_desc('common config files'),
    deb_recommends = 'dlsnode-readonly',
    after_install='pkg/after_install.sh',
)

ARGS = [
    "README.rst=/usr/share/doc/" + VAR.name + "/README.rst",
    "deploy/upstart/dlsreadonly.conf=/etc/init/dlsreadonly.conf",
]

# vim: set ft=python et sw=4 sts=4 :
